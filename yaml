
---

### ✅ **3. Subir pro GitHub**

No terminal:

```bash
git init
git add .
git commit -m "Case de segurança na AWS com Terraform + OPA/Rego"
git branch -M main
git remote add origin https://github.com/tiago1992/terraform-aws-security-opa.git
git push -u origin main
